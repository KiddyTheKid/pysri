[project]
name = "pysri"
version = "0.1.0"
description = "Sistema de facturación electronica basado en FSri de XaviMontero"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.5",
    "django-filter>=25.2",
    "djangorestframework>=3.17.1",
    "markdown>=3.10.2",
]
